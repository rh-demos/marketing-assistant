apiVersion: v1
kind: ConfigMap
metadata:
  name: campaign-director-config
  labels:
    app: campaign-director
data:
  PORT: "8088"
  AGENT_ENDPOINT: "http://campaign-director:8088"
  CREATIVE_PRODUCER_URL: "http://creative-producer:8086"
  CUSTOMER_ANALYST_URL: "http://customer-analyst:8084"
  DELIVERY_MANAGER_URL: "http://delivery-manager:8087"
  POLICY_GUARDIAN_URL: "http://policy-guardian:8085"
  EVENT_HUB_URL: "http://event-hub:8080"
  CLUSTER_DOMAIN: "<TODO>"
  DEV_NAMESPACE: "marketing-dev"
  PROD_NAMESPACE: "marketing-prod"
  CAMPAIGN_API_URL: "http://campaign-api:8089"
  CONFIG_SERVICE_URL: "http://config-service:8081"
  LOG_LEVEL: "INFO"
  OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector.mlflow.svc.cluster.local:4318"
  OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: "http://otel-collector.mlflow.svc.cluster.local:4318/v1/traces"
  OTEL_EXPORTER_OTLP_TRACES_PROTOCOL: "http/protobuf"
  MLFLOW_TRACKING_URI: "http://mlflow-service.mlflow.svc.cluster.local:5000"
  MLFLOW_EXPERIMENT_NAME: "marketing-assistant"
  OTEL_SERVICE_NAME: "campaign-director"
  OTEL_METRICS_EXPORTER: "none"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-director
  labels:
    app: campaign-director
    app.kubernetes.io/name: campaign-director
    app.kubernetes.io/component: agent
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: campaign-director
  template:
    metadata:
      labels:
        app: campaign-director
    spec:
      runtimeClassName: kata
      serviceAccountName: campaign-director
      containers:
      - name: campaign-director
        image: quay.io/rh-demos/marketing-assistant/campaign-director:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8088
          protocol: TCP
        envFrom:
        - configMapRef:
            name: campaign-director-config
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1536Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8088
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8088
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-director
  labels:
    app: campaign-director
spec:
  selector:
    app: campaign-director
  ports:
  - name: http
    port: 8088
    targetPort: 8088
    protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: campaign-director
